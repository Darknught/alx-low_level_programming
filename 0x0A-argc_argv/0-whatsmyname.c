#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints its name
 * @argc: argument to count
 * @argv: arguments
 * Return: 0 (success)
 */
int main(int argc, char **argv)
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
